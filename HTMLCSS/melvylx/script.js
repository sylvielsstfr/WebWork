// write a lot of stuff here
console.log("Hello World");

// write a function to get the current date
function getCurrentDate() {
  return new Date();
}

// write a function to get the current time
function getCurrentTime() {
  return new Date();
}

// write a function to get the current date and time
function getCurrentDateTime() {
  return new Date();
}

// write a function to get the current date and time in a specific format
function getCurrentDateTimeInFormat(format) {
  return new Date().toLocaleString(format);
}

// write a function to get the current date and time in a specific format
function getCurrentDateTimeInFormat(format) {
  return new Date().toLocaleString(format);
}

// write a function to get the current date and time in a specific format
function getCurrentDateTimeInFormat(format) {
  return new Date().toLocaleString(format);
}

// write a function to get the current date and time in a specific format
function getCurrentDateTimeInFormat(format) {
  return new Date().toLocaleString(format);
}
